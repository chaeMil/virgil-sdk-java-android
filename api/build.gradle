publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            pom {
                name = 'Virgil SDK - API'
            }
            artifactId = "api"
        }
    }
}
